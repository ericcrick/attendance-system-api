# Attendance System Monorepo

## Projects
- `backend/` → NestJS API
- `kiosk/` → Frontend
- `admin-portal/` → Admin dashboard

Each project runs independently with its own `package.json`.

### Run backend
```bash
cd backend
npm run start:dev







